export default function WalletPage() {
    return (
        <section>
            <h1 className="text-2xl font-semibold">My Wallet</h1>
            <p className="mt-2 text-sm text-muted-foreground">Escrow wallet skeleton (available / held balance).</p>
        </section>
    );
}
